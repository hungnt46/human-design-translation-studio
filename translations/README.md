# Translations

Store final reader-facing translations under a directory named with the target language code:

```text
translations/<target-language-code>/<filename>
```

Examples: `translations/vi/`, `translations/en/`, `translations/ja/`, and `translations/de/`.

Intermediate chunks, extraction output, and temporary QA material belong under `work/` or `qa/`, not here.
